name: Release

# Builds standalone desktop-app binaries for Linux, Windows and macOS and
# attaches them (plus the Python wheel/sdist) to the GitHub Release for a tag.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and attach assets to (e.g. v0.1.0)"
        required: true
        default: "v0.1.0"

permissions:
  contents: write

jobs:
  build-app:
    name: Build app (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: mobiot-linux-x86_64
            ext: ""
          - os: windows-latest
            artifact: mobiot-windows-x86_64
            ext: ".exe"
          - os: macos-latest
            artifact: mobiot-macos-arm64
            ext: ""
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system libraries for Qt (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libgl1 libxkbcommon0 libdbus-1-3

      - name: Install project (GUI) + PyInstaller
        run: |
          python -m pip install --upgrade pip
          pip install ".[gui]" pyinstaller

      - name: Build standalone binary
        run: >
          pyinstaller --noconfirm --clean --onefile --windowed
          --name mobiot
          --collect-all mobiot
          packaging/mobiot_app.py

      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p out
          cp "dist/mobiot${{ matrix.ext }}" "out/${{ matrix.artifact }}${{ matrix.ext }}"

      - name: Resolve tag
        id: tag
        shell: bash
        run: echo "name=${{ github.event.inputs.tag || github.ref_name }}" >> "$GITHUB_OUTPUT"

      - name: Attach binary to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          files: out/${{ matrix.artifact }}${{ matrix.ext }}
          fail_on_unmatched_files: true

  build-python:
    name: Build wheel + sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build distributions
        run: |
          python -m pip install --upgrade pip build
          python -m build
      - name: Resolve tag
        id: tag
        shell: bash
        run: echo "name=${{ github.event.inputs.tag || github.ref_name }}" >> "$GITHUB_OUTPUT"
      - name: Attach wheel + sdist to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          files: |
            dist/*.whl
            dist/*.tar.gz
